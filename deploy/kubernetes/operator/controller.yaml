apiVersion: v1
kind: ConfigMap
metadata:
  name: coco-operator-controller
  namespace: coco-system
data:
  controller.yaml: |
    # Kubernetes Operator Controller Configuration
    # This controller watches Sandbox CRD and creates sandboxes in the Coco cluster
